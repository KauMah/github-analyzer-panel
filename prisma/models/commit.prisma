model Commit {
  repo         String
  username     String
  hash         String
  timestamp    DateTime
  files        Int
  linesAdded   Int
  linesDeleted Int

  @@id([username, hash])
  @@index([username])
}
